service:
  logger:
    backendType: "terminal"
    backend:
      color: true

  pgClients:
    main:
      uri: "postgres://postgres:postgres@localhost:5432/service"
      applicationName: "service"
      schemaDirectory: "test/pg/schemas"
      schemaNames: ["test"]
